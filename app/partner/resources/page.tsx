import Link from "next/link";import {and,eq,isNull,or} from "drizzle-orm";import {requirePartner} from "@/lib/partner-auth";import {HubNav} from "@/components/partner/hub-nav";import {getDb} from "@/db";import {documents} from "@/db/schema";
export default async function Resources(){const{user}=await requirePartner();const docs=await getDb().select().from(documents).where(or(and(eq(documents.visibility,"private"),user.partnerId?eq(documents.partnerId,user.partnerId):isNull(documents.partnerId)),eq(documents.visibility,"shared")));const privateDocs=docs.filter(d=>d.visibility==="private"),shared=docs.filter(d=>d.visibility==="shared");const list=(items:typeof docs)=>(items.length?<div>{items.map(d=><div className="hub-panel" key={d.id}><span className="hub-eyebrow">{d.category.replaceAll("_"," ")}</span><h3>{d.fileName}</h3><Link className="hub-button" href={`/api/partner/documents/${d.id}`}>Download</Link></div>)}</div>:<div className="hub-empty">Nothing has been published here yet.</div>);return <div className="partner-shell"><HubNav role={user.role}/><main className="hub-main"><span className="hub-eyebrow">Documents & resources</span><h1 className="hub-title">What you need, held securely.</h1><div className="hub-docs"><section className="hub-section"><h2>My Documents</h2>{list(privateDocs)}</section><section className="hub-section"><h2>Resource Library</h2>{list(shared)}</section></div></main></div>}
