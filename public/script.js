
    var manifestArticles = {
      'founders-quarter': {
        pillar: 'The Movement \u2014 Featured',
        title: 'Six Cities, One Calendar: Inside A Founder\u2019s Quarter',
        body: [
          "There is a particular kind of itinerary that never appears on a boarding pass, because no single flight could contain it. It lives instead in a shared calendar \u2014 updated daily, guarded by an assistant, cross-referenced against six time zones \u2014 and it belongs to a person who has stopped thinking of travel as a series of trips and started thinking of it as a single, continuous quarter.",
          "Call her a founder, though the term undersells the breadth of what she runs. A consumer brand with manufacturing in two countries. A board seat on a foundation she takes more seriously than most people take their actual jobs. A small fund that backs three or four companies a year, each demanding its own kind of attention at its own unpredictable moment. The calendar does not separate these roles. It simply lists where she needs to be, and when, and what version of herself the room expects.",
          "Monday begins in one city, where a factory floor needs walking and a supplier relationship needs the kind of repair that only happens face to face. By Wednesday she is somewhere else entirely, in a boardroom where the foundation\u2019s quarterly numbers are being presented, and where her presence \u2014 not her input, necessarily, just her presence \u2014 changes how seriously the numbers are taken. Thursday is a fund meeting that could have been a video call but wasn\u2019t, because the founder she is backing needed to see that someone believed in the company enough to show up. By the weekend she is home, in theory, though home this quarter has relocated twice.",
          "What makes a quarter like this survivable is not stamina, although stamina helps. It is the absence of friction at every point where friction would normally accumulate. The flight that leaves when she is ready rather than when a schedule says she should be. The ground transport that is already moving by the time she clears the door. The destination that has been told, quietly, that this guest prefers not to be photographed and would rather not explain why.",
          "This is the part of private aviation that rarely gets discussed, because it is invisible by design: the difference between a charter and a calendar. A charter solves a single problem \u2014 get from A to B without the indignities of commercial travel. A calendar solves a different problem entirely, which is how a person holding six demanding roles at once keeps each of them from devouring the others. The aircraft is a tool inside that solution, not the solution itself.",
          "There is a temptation, watching someone move through a quarter like this, to assume the operation runs on money. It runs, more accurately, on trust \u2014 the kind built over repeated journeys, where a broker has learned which legs need a fast turnaround and which ones can flex, which destinations require advance diplomatic groundwork and which are simple. That trust is what allows six cities to function as one continuous thought rather than six separate disruptions.",
          "By the time the quarter closes, the calendar has already started filling for the next one. The factory will need walking again. The fund will need another meeting that should have been a call. The foundation\u2019s annual report is due, and the room will expect her in it. None of this resembles rest. All of it resembles a life being run, deliberately, by someone who decided long ago that her time was the one asset she would not let anyone else price for her."
        ]
      },
      'backstage-bridge': {
        pillar: 'The Access',
        title: 'Backstage Before The Bridge',
        body: [
          "Forty minutes before a stadium opens its doors, the building belongs to almost no one \u2014 a skeleton crew checking sightlines, a sound engineer running the same sixteen bars on a loop, a security detail walking a perimeter that will be unrecognisable once eighty thousand people fill it in. This is the hour an artist arrives, and it is, by most accounts, the only quiet the building will know all night.",
          "Getting into that hour is not a matter of arriving early. Stadiums on tour nights run on layered access \u2014 different badges for different rooms, different rooms for different proximities to the stage \u2014 and the gap between landing at the regional airport and standing in the wings is filled with handoffs that have nothing to do with talent and everything to do with logistics. A car that knows which gate to use because the main entrance is already a security bottleneck. A contact backstage who has done this enough times to know which hallway actually leads where the signage says it does.",
          "None of this is exotic. It is, in fact, almost administrative \u2014 the kind of groundwork that succeeds by being unremarkable, by never becoming a story anyone has to tell afterward. The flight in is timed against the show\u2019s actual schedule rather than its printed one, because printed schedules for touring productions are aspirational documents at best.",
          "What makes the difference between an artist who arrives composed and one who arrives frazzled is rarely the flight itself. Most private aviation gets a touring act from city to city without drama; that part is largely solved. The harder problem is the last ninety minutes \u2014 the stretch between wheels-down and walking out in front of eighty thousand people \u2014 where every additional handoff is a chance for something to slip.",
          "Solving for that last ninety minutes means treating it as part of the journey rather than someone else\u2019s problem once the aircraft door opens. It means a broker who has been briefed not just on flight times but on soundcheck times, on load-in schedules, on which member of the touring crew actually has the real information versus which one is repeating what they were told.",
          "The show goes on either way, of course. Touring is relentless in that sense \u2014 the date doesn\u2019t move because the day went sideways. What changes is whether the hour before the bridge feels like preparation or like recovery. Get it right, and nobody backstage even notices there was anything to get right at all."
        ]
      },
      'detail-nobody-asks': {
        pillar: 'The Standard',
        title: 'The Detail Nobody Asks For',
        body: [
          "Most guests never request a particular detergent. They don\u2019t specify cabin temperature on departure versus cabin temperature mid-flight. They certainly don\u2019t ask whether the aircraft they\u2019re about to board was detailed by hand or by a crew rushing between rotations. They simply notice, on some level below conscious attention, when something is slightly off \u2014 and they notice, more quietly still, when nothing is.",
          "This is the strange asymmetry at the centre of luxury service: the things guests will actually mention if they go wrong are rarely the things that took the most work to get right. A delayed departure generates a complaint. A perfectly pressed seam on a seat cushion generates nothing, because it was never meant to be noticed \u2014 it was meant to simply be correct, the way air is meant to simply be breathable.",
          "Consider the cabin itself before a single guest boards. A standard turnaround between flights allows for cleaning. It does not, on its own, guarantee the kind of inspection that catches a scuff on a leather armrest from three flights ago, or a slightly tarnished fixture that\u2019s been quietly degrading since the aircraft entered service. That inspection is a choice \u2014 an additional layer that exists only because someone decided the baseline wasn\u2019t the actual standard, just the starting point for one.",
          "The same logic extends past the cabin door. A jet wash isn\u2019t cosmetic, whatever the word might suggest to someone who has never thought about it. Detailing on this level isn\u2019t about appearances for their own sake \u2014 though appearances matter more than the industry likes to admit \u2014 it\u2019s about catching small issues before they become expensive ones, on an asset where \u201cexpensive\u201d operates on a different scale entirely.",
          "None of this gets photographed for a brochure. The entire category of work that goes into \u201cthe detail nobody asks for\u201d is, definitionally, invisible when it\u2019s done well. It only becomes visible in its absence \u2014 the one time the standard slips, the one flight where something feels just slightly wrong, and the guest can\u2019t quite say what.",
          "That asymmetry is exactly why it matters so much. A guest who has flown privately a hundred times has, by then, an internal calibration for what correct feels like, even if they couldn\u2019t describe it in words. Getting that calibration right, every time, for every guest, regardless of whether anyone is watching closely enough to notice \u2014 that\u2019s the actual job. The detail nobody asks for is the only detail that was ever the point."
        ]
      },
      'jettset-family': {
        pillar: 'The Network',
        title: 'Inside The Jettset Family',
        body: [
          "Ask someone what a private aviation brokerage actually does and most answers stop at the obvious: charter a jet, manage a relationship with a few operators, handle the paperwork a guest shouldn\u2019t have to think about. What that description leaves out is the part that takes years to build and can\u2019t be replicated by simply having capital \u2014 the network of people who make a single phone call solve a problem that would otherwise take a week.",
          "A genuine network in this world isn\u2019t a contact list. It\u2019s closer to a set of relationships with enough history behind them that favours move quickly in both directions. A maintenance crew that will prioritise a last-minute inspection because they trust the broker calling them not to waste their time with false urgency. An FBO manager who will hold a slot during a busy event weekend because the relationship predates the event and will outlast it.",
          "None of this is transactional in the way a one-off booking is transactional. It\u2019s closer to a long, slow accumulation of trust \u2014 the kind that gets tested in small ways constantly and rewarded only when something actually goes wrong and needs fixing fast. A diverted flight at midnight. A guest who needs a different aircraft type with four hours\u2019 notice because their plans changed mid-trip.",
          "The strength of a network like this shows up almost exclusively in moments nobody plans for. Most journeys go exactly as expected \u2014 that\u2019s the baseline, not the achievement. The achievement is what happens on the rare occasion that something doesn\u2019t: a mechanical delay that needs an alternative aircraft sourced within the hour, a weather diversion that needs ground transport rerouted to a different city entirely.",
          "That\u2019s the actual definition of a family in this context. Not sentimentality, and not even loyalty in the abstract sense \u2014 just a working group of people, scattered across cities and time zones, who have decided over years of small interactions that a call from this particular source is one worth answering quickly. The guest never sees the network directly. They simply experience the result: a problem that should have taken a day to solve, solved in twenty minutes."
        ]
      },
      'peak-season-cost': {
        pillar: 'The Brief',
        title: 'What Peak Season Actually Costs',
        body: [
          "Every industry has a season where the math stops behaving the way it does the rest of the year, and private aviation\u2019s version of that season is more visible than most, because it intersects with exactly the events that put pressure on every form of premium travel at once. For a handful of concentrated weeks each year, demand for aircraft doesn\u2019t simply rise \u2014 it reorganises the entire supply chain around it.",
          "The mechanics are straightforward enough to explain and complicated enough to actually navigate. Aircraft availability in any given region is finite at any given moment; on a normal week, that finite supply comfortably meets demand. During an event week, every operator with capacity in range of the destination is fielding requests simultaneously, and the aircraft that would have been available with two days\u2019 notice in March is fully committed with two weeks\u2019 notice in May.",
          "What this means in practice is that the guests who fly comfortably through peak season are, almost without exception, the ones who started planning before the season was visible to everyone else. Six weeks ahead of a major event, the right aircraft type is still available at a sensible price. Two weeks ahead, the same itinerary costs meaningfully more and offers meaningfully less choice.",
          "There\u2019s a temptation to read this as private aviation behaving badly during high season \u2014 charging more because it can. That reading misses what\u2019s actually happening underneath the price. The aircraft itself hasn\u2019t changed. What\u2019s changed is the value of certainty during a window where uncertainty is genuinely expensive.",
          "The guests who understand this best don\u2019t treat peak season as an exception to plan around reactively. They treat it as a known variable, built into how far ahead they think about travel generally. They ask in February, when the answers are still flexible and the pricing still reflects a market that hasn\u2019t yet been forced to choose between equally insistent requests."
        ]
      },
      'recovery-windows': {
        pillar: 'The Movement',
        title: 'Recovery Windows, Explained',
        body: [
          "The flight that gets an athlete to a city on time is, in most conversations about sports logistics, the only flight that matters. The flight home \u2014 the one that happens after the competition, after the adrenaline, after the body has spent everything it had \u2014 gets treated, far too often, as an afterthought. That\u2019s a mistake the people who actually understand athletic performance stopped making years ago.",
          "Recovery isn\u2019t a single moment that starts once an athlete steps off a podium or out of a locker room. It\u2019s a window \u2014 physiologically real, measurable, and finite \u2014 during which the choices made about rest, nutrition, and particularly sleep have a disproportionate effect on how quickly a body returns to baseline. Compress that window with a difficult travel experience, and the cost shows up days later.",
          "A return flight built around recovery looks different from a return flight built around simply getting somewhere. The departure time accounts for how long the athlete actually needs after the event ends, rather than the earliest slot that happened to be available. The cabin environment is considered deliberately \u2014 lighting that supports rather than disrupts a body trying to recalibrate.",
          "None of this is exotic science. What\u2019s harder than knowing it is actually building it into a travel plan that has dozens of other constraints layered on top \u2014 team schedules, media obligations, the next competition already looming on the calendar. The flight home becomes one variable among many, and it takes deliberate effort to make sure it isn\u2019t the one that gets sacrificed.",
          "The teams and individual athletes who get this right tend to share one trait: they stopped treating the return journey as logistics and started treating it as part of training. The result, when it\u2019s done properly, is almost invisible. Nobody writes a headline about a well-managed recovery flight \u2014 just about the athlete who came back strong enough to perform at the next one."
        ]
      },
      'fbos-nobody-photographs': {
        pillar: 'The Access',
        title: 'The FBOs Nobody Photographs',
        body: [
          "Every airport has a public face \u2014 the terminal, the signage, the part built for the widest possible audience to navigate without assistance. Most private aviation happens somewhere else entirely: a separate building, often unmarked from the road, where the entire design philosophy runs in the opposite direction. Not toward visibility, but away from it.",
          "These are fixed-base operators, the dedicated terminals that handle private and business aviation away from commercial traffic, and the best ones share a quality that has nothing to do with luxury finishes or architectural ambition. They are quiet. Not quiet in d\u00e9cor \u2014 quiet in function. A guest can be driven directly to their aircraft without walking through a single shared space.",
          "This matters more than it might initially seem to, because privacy in this context isn\u2019t about secrecy for its own sake. It\u2019s about removing the specific kind of friction that comes from being seen, recognised, or simply observed during a moment when a guest would rather not be performing for anyone.",
          "The mechanics behind that kind of privacy are almost entirely about sequencing. A guest\u2019s arrival is timed against the aircraft\u2019s readiness, not the other way around, so there\u2019s no waiting room, no holding pattern, no moment where a person stands in a semi-public space with nothing to do but be looked at.",
          "What makes the best FBOs genuinely good at this isn\u2019t expensive interiors, although many of them have those too. It\u2019s discipline \u2014 the kind that shows up in a thousand small operational decisions nobody outside the industry ever thinks about. None of these decisions photograph well, because the entire point is that the guest experiencing it well will never describe it to anyone \u2014 there was nothing dramatic enough to mention."
        ]
      }
    };

    function openArticle(id){
      var article = manifestArticles[id];
      if(!article) return;
      document.getElementById('articlePillar').textContent = article.pillar;
      document.getElementById('articleTitle').textContent = article.title;
      document.title = article.title + ' — The Manifest — Jettset Global';
      var bodyEl = document.getElementById('articleBody');
      bodyEl.innerHTML = '';
      article.body.forEach(function(paragraph){
        var p = document.createElement('p');
        p.textContent = paragraph;
        bodyEl.appendChild(p);
      });
    }

    // Full-screen editorial index (Design Law Rule 1) — a single mark does
    // every job: opens the index, and its own +/x rotation is the only
    // open/closed signal. No separate close button, no burger label.
    var navToggle = document.getElementById('navBurgerOpen');
    function openSidePanel(){
      document.getElementById('sidePanel').classList.add('is-open');
      document.getElementById('sideOverlay').classList.add('is-open');
      document.getElementById('mainNav').classList.add('menu-open');
      navToggle.setAttribute('aria-expanded', 'true');
      navToggle.setAttribute('aria-label', 'Close menu');
      document.body.style.overflow = 'hidden';
    }
    function closeSidePanel(){
      document.getElementById('sidePanel').classList.remove('is-open');
      document.getElementById('sideOverlay').classList.remove('is-open');
      document.getElementById('mainNav').classList.remove('menu-open');
      navToggle.setAttribute('aria-expanded', 'false');
      navToggle.setAttribute('aria-label', 'Open menu');
      document.body.style.overflow = '';
    }
    function isSidePanelOpen(){
      return document.getElementById('sidePanel').classList.contains('is-open');
    }

    navToggle.addEventListener('click', function(e){
      e.preventDefault();
      if(isSidePanelOpen()){ closeSidePanel(); } else { openSidePanel(); }
    });
    document.getElementById('sideOverlay').addEventListener('click', closeSidePanel);
    // The index is full-screen, so there is no exposed backdrop area left for
    // the old "click outside to close" pattern -- instead, clicking anywhere
    // in the panel's negative space (not on a link or button) closes it.
    document.getElementById('sidePanel').addEventListener('click', function(e){
      if(!e.target.closest('a, button')){
        closeSidePanel();
      }
    });
    document.addEventListener('keydown', function(e){
      if(e.key === 'Escape' && isSidePanelOpen()){
        closeSidePanel();
      }
    });

    document.getElementById('navMemberLogin').addEventListener('click', function(e){
      e.preventDefault();
      alert('The Legs member portal is a separate, secure login area. Ask your assistant to open the Legs member portal to access it.');
    });

    // The old jbSubmit/jbDestination (a persistent full-width destination
    // input on every page) is retired -- the journey mark on the homepage
    // is now a plain anchor to the Journey Builder section, using the
    // sitewide scroll-behavior:smooth already in place, no JS needed here.

    // .nav-enquire (the persistent bar's old CTA) no longer exists in the DOM
    // -- removed from the bar per Design Law Rule 1 (single mark + one
    // toggle). Its "open a Call/WhatsApp/Email sheet on mobile" behaviour is
    // now redundant: Call and WhatsApp are permanent, direct links inside
    // the full-screen index (.nav-index-contact), reachable in one tap
    // rather than hidden behind a second sheet. The contactSheetOverlay
    // markup itself is left in the page (harmless, unreachable) rather than
    // stripped from 22 files in the same pass as this fix -- flagged as a
    // follow-up cleanup rather than expanding this change further.
    (function(){
      var sheet = document.getElementById('contactSheetOverlay');
      if(!sheet) return;
      sheet.addEventListener('click', function(e){
        if(e.target === sheet) sheet.classList.remove('is-active');
      });
      var cancelBtn = document.getElementById('contactSheetCancel');
      if(cancelBtn){
        cancelBtn.addEventListener('click', function(){ sheet.classList.remove('is-active'); });
      }
      document.addEventListener('keydown', function(e){
        if(e.key === 'Escape') sheet.classList.remove('is-active');
      });
    })();

    // ===== FLEET CATEGORY MODAL =====
    (function(){
      var overlay = document.getElementById('fleetModalOverlay');
      var cards = document.querySelectorAll('.category-card');
      if(!overlay || !cards.length) return;

      var modal = overlay.querySelector('.fleet-modal');
      var titleEl = document.getElementById('fleetModalTitle');
      var metaEl = document.getElementById('fleetModalMeta');
      var descEl = document.getElementById('fleetModalDesc');
      var aircraftEl = document.getElementById('fleetModalAircraft');

      // Aircraft imagery per category. Add new entries here as fresh (unbranded)
      // quotes come in -- no HTML changes needed elsewhere.
      var fleetData = {
        'vlj-light': {
          aircraft: [
            {
              name:'Citation CJ3', exterior:'images/fleet/cj3_ext.jpg', interior:'images/fleet/cj3_int.jpg', diagram:'images/fleet/diagrams/cj3_diagram.svg',
              specs:{lengthM:4.78, widthM:1.47, heightM:1.45, seats:'Up to 8', length:'4.78m (15\'8")', width:'1.47m (4\'10")', height:'1.45m (4\'9")', range:'3,500km / 1,890nm', speed:'770km/h'},
              features:{catering:'Standard catering included; your Concierge can arrange a bespoke menu, dietary request, or occasion styling in advance.', wifi:'Available on select aircraft only — confirmed by your Jettset team when your aircraft is sourced.', wc:'Yes, fully enclosed', luggage:'6 items, 20kg max', smoking:'No', flightAttendant:'Not standard on short regional journeys — available on request when your aircraft is sourced.'}
            },
            {
              name:'Citation XLS', exterior:'images/fleet/citation_xls_ext.jpg', interior:'images/fleet/citation_xls_int.jpg', diagram:'images/fleet/diagrams/citation_xls_diagram.png',
              specs:{lengthM:5.7, widthM:1.68, heightM:1.73, seats:'Up to 8', length:'5.7m (18\'7")', width:'1.68m (5\'5")', height:'1.73m (5\'8")', range:'3,280km / 1,770nm', speed:'816km/h'},
              features:{catering:'Standard catering included; your Concierge can arrange a bespoke menu, dietary request, or occasion styling in advance.', wifi:'Available on select aircraft only — confirmed by your Jettset team when your aircraft is sourced.', wc:'Yes, fully enclosed', luggage:'8 items, 20kg max', smoking:'No', flightAttendant:'Not standard on short regional journeys — available on request when your aircraft is sourced.'},
              note:'Manufactured 2004 / Refurbished 2021'
            },
            {
              name:'Pilatus PC-24', exterior:'images/fleet/pilatus_pc24_ext.jpg', interior:'images/fleet/pilatus_pc24_int.jpg', diagram:'images/fleet/diagrams/pilatus_pc24_diagram.png',
              specs:{lengthM:7.01, widthM:1.69, heightM:1.55, seats:'Up to 8', length:'7.01m (23\'0")', width:'1.69m (5\'6")', height:'1.55m (5\'1")', range:'3,700km / 2,000nm', speed:'815km/h'},
              features:{catering:'Standard catering included; your Concierge can arrange a bespoke menu, dietary request, or occasion styling in advance.', wifi:'Available on select aircraft only — confirmed by your Jettset team when your aircraft is sourced.', wc:'Yes, fully enclosed', luggage:'9 items, 20kg max', smoking:'No', flightAttendant:'Not standard on short regional journeys — available on request when your aircraft is sourced.'},
              note:'Manufactured 2021+ / Floating fleet'
            }
          ]
        },
        'midsize': {
          aircraft: [
            {
              name:'Citation Latitude', exterior:'images/fleet/citation_latitude_ext.jpg', interior:'images/fleet/citation_latitude_int.jpg', diagram:'images/fleet/diagrams/citation_latitude_diagram.png',
              specs:{lengthM:6.63, widthM:1.96, heightM:1.83, seats:'Up to 8', length:'6.63m (21\'8")', width:'1.96m (6\'5")', height:'1.83m (6\'0")', range:'5,280km / 2,850nm', speed:'826km/h'},
              features:{catering:'Standard catering included; your Concierge can arrange a bespoke menu, dietary request, or occasion styling in advance.', wifi:'Available on select aircraft only — confirmed by your Jettset team when your aircraft is sourced.', wc:'Yes, fully enclosed', luggage:'10 items, 20kg max', smoking:'No', flightAttendant:'Available on request for longer journeys, confirmed when your aircraft is sourced.', ownerApproval:'Yes'},
              note:'Manufactured 2017'
            },
            {
              name:'Praetor 600', exterior:'images/fleet/praetor600_ext.jpg', interior:'images/fleet/praetor600_int.jpg', diagram:'images/fleet/diagrams/praetor600_diagram.png',
              specs:{lengthM:8.08, widthM:2.08, heightM:1.83, seats:'Up to 9', length:'8.08m (26\'6")', width:'2.08m (6\'10")', height:'1.83m (6\'0")', range:'6,780km / 3,700nm', speed:'863km/h'},
              features:{catering:'Standard catering included; your Concierge can arrange a bespoke menu, dietary request, or occasion styling in advance.', wifi:'Available on select aircraft only — confirmed by your Jettset team when your aircraft is sourced.', wc:'Yes, fully enclosed', luggage:'14 items, 20kg max', smoking:'No', flightAttendant:'Available on request for longer journeys, confirmed when your aircraft is sourced.', ownerApproval:'Yes'},
              note:'Manufactured 2019'
            }
          ]
        },
        'heavy': {
          aircraft: [
            {
              name:'Challenger 604', exterior:'images/fleet/challenger604_ext.jpg', interior:'images/fleet/challenger604_int.jpg', diagram:'images/fleet/diagrams/challenger604_diagram.png',
              specs:{lengthM:8.6, widthM:2.48, heightM:1.83, seats:'Up to 10', length:'8.6m (28\'2")', width:'2.48m (8\'1")', height:'1.83m (6\'0")', range:'7,450km / 4,024nm', speed:'904km/h'},
              features:{catering:'Standard catering included; your Concierge can arrange a bespoke menu, dietary request, or occasion styling in advance.', wifi:'Available on select aircraft only — confirmed by your Jettset team when your aircraft is sourced.', wc:'Yes, fully enclosed', luggage:'14 items, 20kg max', smoking:'No', flightAttendant:'Standard on most journeys of this class.'},
              note:'Manufactured 2004+ / Refurbished 2021+ / Floating fleet'
            }
          ]
        },
        'super-midsize': {
          // No real quote-sourced photos/specs for this category yet --
          // deliberately left empty rather than reusing 'midsize' data
          // (a different aircraft class) or inventing figures. Renders
          // the same honest empty-state as 'helicopter' below, until
          // real sourced aircraft are added here.
          aircraft: []
        },
        'ulr': {
          aircraft: [
            {
              name:'Gulfstream G550', exterior:'images/fleet/g550_ext.jpg', interior:'images/fleet/g550_int.jpg', diagram:'images/fleet/diagrams/g550_diagram.png',
              specs:{lengthM:15.27, widthM:2.24, heightM:1.88, seats:'Up to 14', length:'15.27m (50\'1")', width:'2.24m (7\'4")', height:'1.88m (6\'2")', range:'12,500km / 6,750nm', speed:'904km/h'},
              features:{catering:'Standard catering included; your Concierge can arrange a bespoke menu, dietary request, or occasion styling in advance.', wifi:'Available on select aircraft only — confirmed by your Jettset team when your aircraft is sourced.', wc:'Yes, fully enclosed', luggage:'18 items, 20kg max', smoking:'No', flightAttendant:'Standard on board, given the length of these journeys.'},
              note:'Manufactured 2013'
            },
            {
              name:'Global 7500', exterior:'images/fleet/global7500_ext.jpg', interior:'images/fleet/global7500_int.jpg', diagram:'images/fleet/diagrams/global7500_diagram.png',
              specs:{lengthM:16.59, widthM:2.41, heightM:1.88, seats:'Up to 12', length:'16.59m (54\'4")', width:'2.41m (7\'11")', height:'1.88m (6\'2")', range:'14,260km / 7,700nm', speed:'956km/h'},
              features:{catering:'Standard catering included; your Concierge can arrange a bespoke menu, dietary request, or occasion styling in advance.', wifi:'Available on select aircraft only — confirmed by your Jettset team when your aircraft is sourced.', wc:'Yes, fully enclosed', luggage:'22 items, 20kg max', smoking:'No', flightAttendant:'Standard on board, given the length of these journeys.'},
              note:'Manufactured 2023'
            }
          ]
        },
        'group-charter': {
          aircraft: [
            {
              name:'Boeing Business Jet', exterior:'images/fleet/bbj_ext.jpg', interior:'images/fleet/bbj_lounge.jpg', diagram:'images/fleet/diagrams/bbj_diagram.svg',
              specs:{lengthM:24.1, widthM:3.53, heightM:2.16, seats:'Up to 20', length:'24.1m (79\'2")', width:'3.53m (11\'7")', height:'2.16m (7\'1")', range:'11,500km / 6,200nm', speed:'870km/h'},
              features:{catering:'Standard catering included; your Concierge can arrange a bespoke menu, dietary request, or occasion styling in advance.', wifi:'Available on select aircraft only — confirmed by your Jettset team when your aircraft is sourced.', wc:'Multiple, fully enclosed', luggage:'By arrangement, full cargo hold', smoking:'No', flightAttendant:'Full cabin crew, standard.'}
            }
          ]
        },
        'helicopter': {
          aircraft: []
        }
      };

      // Longest cabin among all aircraft, used to scale the dimension diagram consistently
      var MAX_CABIN_LENGTH_M = 16.60; // Global 7500 -- BBJ excluded, it's an outlier airliner cabin

      function buildDimensionDiagram(plane){
        var specs = plane.specs;
        var wrap = document.createElement('div');
        wrap.className = 'fleet-dim-diagram';

        if(plane.diagram){
          var layoutImg = document.createElement('img');
          layoutImg.className = 'fleet-layout-diagram';
          layoutImg.src = plane.diagram;
          layoutImg.alt = plane.name + ' cabin layout diagram: seating configuration and cross-section';
          layoutImg.loading = 'lazy';
          wrap.appendChild(layoutImg);
        }

        var lengthPct = Math.min(100, (specs.lengthM / MAX_CABIN_LENGTH_M) * 100);

        var lengthRow = document.createElement('div');
        lengthRow.className = 'fleet-dim-length-row';
        var track = document.createElement('div');
        track.className = 'fleet-dim-length-track';
        var bar = document.createElement('div');
        bar.className = 'fleet-dim-length-bar';
        bar.style.width = lengthPct + '%';
        track.appendChild(bar);
        lengthRow.appendChild(track);
        wrap.appendChild(lengthRow);

        var labels = document.createElement('div');
        labels.className = 'fleet-dim-labels';
        labels.innerHTML =
          '<span><b>L</b> ' + specs.length + '</span>' +
          '<span><b>W</b> ' + specs.width + '</span>' +
          '<span><b>H</b> ' + specs.height + '</span>';
        wrap.appendChild(labels);

        return wrap;
      }

      function buildSpecList(specs){
        var list = document.createElement('div');
        list.className = 'fleet-spec-list';
        var rows = [
          ['Seating capacity', specs.seats],
          ['Cabin length', specs.length],
          ['Cabin width', specs.width],
          ['Cabin height', specs.height],
          ['Range', specs.range],
          ['Cruise speed', specs.speed]
        ];
        rows.forEach(function(r){
          var row = document.createElement('div');
          row.className = 'fleet-spec-row';
          row.innerHTML = '<span>' + r[0] + '</span><span>' + r[1] + '</span>';
          list.appendChild(row);
        });
        return list;
      }

      function buildFeatureList(features){
        var list = document.createElement('div');
        list.className = 'fleet-feature-list';
        var rows = [
          ['Catering', features.catering],
          ['WiFi', features.wifi],
          ['WC', features.wc],
          ['Luggage', features.luggage],
          ['Smoking', features.smoking],
          ['Flight attendant', features.flightAttendant],
          ['Owner approval', features.ownerApproval]
        ];
        rows.forEach(function(r){
          if(!r[1]) return;
          var row = document.createElement('div');
          row.className = 'fleet-feature-row';
          row.innerHTML = '<b>' + r[0] + '</b><span>' + r[1] + '</span>';
          list.appendChild(row);
        });
        return list;
      }

      function openModal(card){
        var categoryId = card.getAttribute('data-category');
        var data = fleetData[categoryId];
        var title = card.querySelector('h4').textContent;
        var meta = card.querySelector('.category-meta').textContent;
        var desc = card.querySelector('p').textContent;

        titleEl.textContent = title;
        metaEl.textContent = meta;
        descEl.textContent = desc;

        aircraftEl.innerHTML = '';
        var list = (data && data.aircraft) ? data.aircraft : [];
        if(list.length){
          modal.classList.remove('is-empty');
          list.forEach(function(plane){
            var card2 = document.createElement('div');
            card2.className = 'fleet-aircraft-card';

            var media = document.createElement('div');
            media.className = 'fleet-aircraft-media';

            function buildPane(src, label, altSuffix){
              var pane = document.createElement('div');
              pane.className = 'fleet-aircraft-pane';
              var img = document.createElement('img');
              img.src = src;
              img.alt = plane.name + ' ' + altSuffix + ', ' + title + ' category, Jettset Global charter fleet';
              img.loading = 'lazy';
              var cap = document.createElement('span');
              cap.className = 'fleet-aircraft-pane-label';
              cap.textContent = label;
              pane.appendChild(img);
              pane.appendChild(cap);
              return pane;
            }

            media.appendChild(buildPane(plane.exterior, 'Exterior', 'exterior'));
            if(plane.interior){
              media.appendChild(buildPane(plane.interior, 'Interior', 'cabin interior'));
            }

            var nameWrap = document.createElement('div');
            nameWrap.className = 'fleet-aircraft-name';
            var eyebrow = document.createElement('span');
            eyebrow.className = 'eyebrow';
            eyebrow.textContent = 'Aircraft';
            var h5 = document.createElement('h5');
            h5.textContent = plane.name;
            nameWrap.appendChild(eyebrow);
            nameWrap.appendChild(h5);
            if(plane.note){
              var noteEl = document.createElement('span');
              noteEl.className = 'fleet-aircraft-note';
              noteEl.textContent = plane.note;
              nameWrap.appendChild(noteEl);
            }

            card2.appendChild(media);
            card2.appendChild(nameWrap);

            if(plane.specs || plane.features){
              var detail = document.createElement('div');
              detail.className = 'fleet-aircraft-detail';

              var specsCol = document.createElement('div');
              specsCol.className = 'fleet-detail-col';
              var specsLabel = document.createElement('span');
              specsLabel.className = 'fleet-detail-label';
              specsLabel.textContent = 'Specifications';
              specsCol.appendChild(specsLabel);
              if(plane.specs){
                specsCol.appendChild(buildDimensionDiagram(plane));
                specsCol.appendChild(buildSpecList(plane.specs));
              }

              var featuresCol = document.createElement('div');
              featuresCol.className = 'fleet-detail-col';
              var featuresLabel = document.createElement('span');
              featuresLabel.className = 'fleet-detail-label';
              featuresLabel.textContent = 'Aircraft Features';
              featuresCol.appendChild(featuresLabel);
              if(plane.features){
                featuresCol.appendChild(buildFeatureList(plane.features));
              }

              detail.appendChild(specsCol);
              detail.appendChild(featuresCol);
              card2.appendChild(detail);
            }

            aircraftEl.appendChild(card2);
          });
        } else {
          modal.classList.add('is-empty');
        }

        overlay.classList.add('is-active');
      }

      function closeModal(){
        overlay.classList.remove('is-active');
      }

      cards.forEach(function(card){
        card.setAttribute('tabindex', '0');
        card.setAttribute('role', 'button');
        card.addEventListener('click', function(){ openModal(card); });
        card.addEventListener('keydown', function(e){
          if(e.key === 'Enter' || e.key === ' '){ e.preventDefault(); openModal(card); }
        });
      });

      document.getElementById('fleetModalClose').addEventListener('click', closeModal);
      overlay.addEventListener('click', function(e){
        if(e.target === overlay) closeModal();
      });
      document.addEventListener('keydown', function(e){
        if(e.key === 'Escape') closeModal();
      });
    })();

    document.querySelectorAll('.btn-ghost').forEach(function(btn){
      // Real form-submit buttons must never be hijacked by this generic CTA
      // handler. Bug found during Rule 5 verification: quote.html's own
      // submit button shares both the .btn-ghost class and the exact text
      // "Begin Your Journey" with this handler's match list, so a genuine
      // click was being intercepted, preventDefault()-ed, and redirected to
      // 'quote.html' -- instead of ever reaching the form's real submit
      // handler. The form's own validation/alert/reset logic was correct
      // the whole time; it just never received the click. Excluding any
      // type="submit" button here, not just this one instance, so the same
      // collision can't happen again if button copy changes later.
      if(btn.tagName === 'BUTTON' && btn.type === 'submit') return;
      var text = btn.textContent.trim();
      if(['Enquire Now', 'Begin Your Journey', 'Find Your Fit', 'Start A Conversation'].indexOf(text) !== -1){
        btn.addEventListener('click', function(e){
          if(btn.getAttribute('href') && btn.getAttribute('href') !== '#') return; // real link already, let it navigate
          e.preventDefault();
          window.jtNavigate('quote.html');
        });
      }
    });

    var tripType = 'oneway';
    document.querySelectorAll('#quoteTripToggle button').forEach(function(btn){
      btn.addEventListener('click', function(){
        document.querySelectorAll('#quoteTripToggle button').forEach(function(b){ b.classList.remove('active'); });
        this.classList.add('active');
        tripType = this.getAttribute('data-trip');
        document.getElementById('qReturnField').style.display = (tripType === 'roundtrip') ? 'flex' : 'none';
      });
    });

    var passengerCounts = { adults: 1, children: 0, pets: 0 };
    var passengerLabels = { adults: 'Adults', children: 'Children', pets: 'Pets' };
    document.querySelectorAll('.stepper').forEach(function(stepper){
      var key = stepper.getAttribute('data-counter');
      var span = stepper.querySelector('span');
      stepper.querySelectorAll('button').forEach(function(btn){
        btn.addEventListener('click', function(){
          var action = this.getAttribute('data-action');
          var min = (key === 'adults') ? 1 : 0;
          if(action === 'inc') passengerCounts[key]++;
          else passengerCounts[key] = Math.max(min, passengerCounts[key] - 1);
          var n = passengerCounts[key];
          var label = (key === 'adults' && n === 1) ? 'Adult' : passengerLabels[key];
          span.textContent = n + ' ' + label;
        });
      });
    });

    var quoteFormEl = document.getElementById('quoteForm');
    if(quoteFormEl) quoteFormEl.addEventListener('submit', function(e){
      e.preventDefault();
      var consent = document.getElementById('qConsent').checked;
      var name = document.getElementById('qName').value.trim();
      var email = document.getElementById('qEmail').value.trim();
      if(!consent){
        alert('Please confirm you are happy to be contacted regarding this enquiry before submitting.');
        return;
      }
      if(!name || !email){
        alert('Please share at least your name and email so we can respond.');
        return;
      }
      alert('Thank you, ' + name + '. Your request has been received, and a member of our team will be in touch shortly to discuss your journey.');
      this.reset();
      document.querySelectorAll('.stepper span').forEach(function(span, i){
        var keys = ['adults','children','pets'];
        passengerCounts[keys[i]] = (keys[i] === 'adults') ? 1 : 0;
        span.textContent = passengerCounts[keys[i]] + ' ' + (keys[i] === 'adults' ? 'Adult' : passengerLabels[keys[i]]);
      });
    });

    var partnerFormEl = document.getElementById('partnerForm');
    if(partnerFormEl) partnerFormEl.addEventListener('submit', function(e){
      e.preventDefault();
      var consent = document.getElementById('pConsent').checked;
      var name = document.getElementById('pName').value.trim();
      var email = document.getElementById('pEmail').value.trim();
      if(!consent){
        alert('Please confirm you are happy to be contacted regarding this enquiry before submitting.');
        return;
      }
      if(!name || !email){
        alert('Please share at least your name and email so we can respond.');
        return;
      }
      alert('Thank you, ' + name + '. Your message has been received, and a member of our team will be in touch shortly to continue the conversation.');
      this.reset();
    });

    var enquiryFormEl = document.getElementById('enquiryForm');
    if(enquiryFormEl) enquiryFormEl.addEventListener('submit', function(e){
      e.preventDefault();
      var consent = document.getElementById('eConsent').checked;
      var name = document.getElementById('eName').value.trim();
      var email = document.getElementById('eEmail').value.trim();
      if(!consent){
        alert('Please confirm you are happy to be contacted regarding this enquiry before submitting.');
        return;
      }
      if(!name || !email){
        alert('Please share at least your name and email so we can respond.');
        return;
      }
      alert('Thank you, ' + name + '. We\'ve received your message, and a member of our team will be in touch shortly to understand how we can help.');
      this.reset();
    });

    var legsWaitlistFormEl = document.getElementById('legsWaitlistForm');
    if(legsWaitlistFormEl) legsWaitlistFormEl.addEventListener('submit', function(e){
      e.preventDefault();
      var consent = document.getElementById('lwConsent').checked;
      var name = document.getElementById('lwName').value.trim();
      var email = document.getElementById('lwEmail').value.trim();
      var region = document.getElementById('lwRegion').value;
      if(!consent){
        alert('Please confirm you are happy to be contacted as Legs access opens before submitting.');
        return;
      }
      if(!name || !email || !region){
        alert('Please share your name, email, and region so we can notify you at the right time.');
        return;
      }
      alert('Thank you, ' + name + '. You\'re on the Legs waitlist — we\'ll be in touch as soon as access opens for ' + region + '.');
      this.reset();
    });

    var jetCardFormEl = document.getElementById('jetCardForm');
    if(jetCardFormEl) jetCardFormEl.addEventListener('submit', function(e){
      e.preventDefault();
      var consent = document.getElementById('jcConsent').checked;
      var name = document.getElementById('jcName').value.trim();
      var email = document.getElementById('jcEmail').value.trim();
      if(!consent){
        alert('Please confirm you are happy to be contacted regarding Jet Card before submitting.');
        return;
      }
      if(!name || !email){
        alert('Please share at least your name and email so we can respond.');
        return;
      }
      alert('Thank you, ' + name + '. A member of our Jet Card team will be in touch shortly.');
      this.reset();
    });

    window.addEventListener('scroll', function(){
      var nav = document.getElementById('mainNav');
      if(window.scrollY > 60){ nav.classList.add('scrolled'); }
      else{ nav.classList.remove('scrolled'); }

      // journeyBar only exists on the homepage now (Chin: keep this mark
      // off interior pages, which already have their own in-content CTAs)
      var jb = document.getElementById('journeyBar');
      if(jb){
        if(window.scrollY > window.innerHeight * 0.6){ jb.classList.add('is-visible'); }
        else{ jb.classList.remove('is-visible'); }
      }
    });

    // ===== HERO VIDEOS: respect reduced motion =====
    (function(){
      var videos = document.querySelectorAll('.hero-video');
      if(!videos.length) return;
      var reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      if(reduceMotion){
        videos.forEach(function(video){
          video.removeAttribute('autoplay');
          video.pause();
          video.currentTime = 0;
        });
      }
    })();

    // ===== CONCIERGE: GROUND FLEET SELECTOR =====
    (function(){
      var tabsWrap = document.getElementById('fleetTabs');
      var panel = document.getElementById('fleetPanel');
      if(!tabsWrap || !panel) return;

      var uses = {
        individuals: {
          title:'Door-To-Door, Discreetly',
          text:'Door-to-door airport transfers, full-day chauffeur hire, discreet principal transport, family travel and multi-stop itineraries.',
          vehicles:['Rolls-Royce Cullinan','Range Rover Autobiography','Mercedes-Benz S-Class']
        },
        touring: {
          title:'City-By-City, Coordinated',
          text:'City-by-city movement plans, VIP Sprinters, V-Class fleets, luggage vehicles, driver manifests, call-time coordination and live operational support.',
          vehicles:['Mercedes-Maybach V-Class','Custom VIP Sprinters','Executive Minibuses','Luggage Support Vehicles']
        },
        events: {
          title:'Guest Movement, At Scale',
          text:'Guest movement, executive transport, delegations, brand activations, hospitality programmes and high-volume arrivals.',
          vehicles:['Executive Saloons','VIP Sprinters','Luggage Vehicles','Security Vehicles']
        },
        aviation: {
          title:'Seamless, Start To Finish',
          text:'Seamless movement between home, hotel, private terminal, commercial airport and onward destination, coordinated against the live itinerary.',
          vehicles:['Executive Saloons','Mercedes-Maybach V-Class','Accessible Transport']
        }
      };

      function render(use){
        var u = uses[use];
        panel.innerHTML = '<h4>' + u.title + '</h4><p>' + u.text + '</p>' +
          '<div class="fleet-panel-vehicles">' + u.vehicles.map(function(v){ return '<span class="fleet-vehicle-chip">' + v + '</span>'; }).join('') + '</div>';
      }

      tabsWrap.querySelectorAll('.fleet-tab').forEach(function(tab){
        tab.addEventListener('click', function(){
          tabsWrap.querySelectorAll('.fleet-tab').forEach(function(t){ t.classList.remove('is-active'); });
          tab.classList.add('is-active');
          render(tab.dataset.use);
        });
      });

      render('individuals');
    })();

    // ===== JET CARD: REGION SELECTOR =====
    (function(){
      var tabsWrap = document.getElementById('regionTabs');
      var chipsWrap = document.getElementById('regionChips');
      if(!tabsWrap || !chipsWrap) return;

      var regions = {
        europe: ['United Kingdom','France','Germany','Switzerland','Italy','Spain','Portugal','Greece','Nordic Countries','Central & Eastern Europe'],
        us: ['All 50 States','Alaska','Hawaii','Canada','Mexico','Puerto Rico','U.S. Virgin Islands','Core Caribbean'],
        me: ['United Arab Emirates','Saudi Arabia','Qatar','Kuwait','Oman','Bahrain','Jordan','Egypt','Israel','The Maldives','The Seychelles']
      };

      function render(region){
        chipsWrap.innerHTML = regions[region].map(function(c){ return '<span class="region-chip">' + c + '</span>'; }).join('');
      }

      tabsWrap.querySelectorAll('.region-tab').forEach(function(tab){
        tab.addEventListener('click', function(){
          tabsWrap.querySelectorAll('.region-tab').forEach(function(t){ t.classList.remove('is-active'); });
          tab.classList.add('is-active');
          render(tab.dataset.region);
        });
      });

      render('europe');
    })();

    // ===== LEGS: LIVE OPPORTUNITY TICKER =====
    (function(){
      var track = document.getElementById('legsTickerTrack');
      if(!track) return;

      var opportunities = [
        {route:'Nice &#8594; London', cat:'Midsize Jet', price:'~45% below charter rate', window:'Departs today, 18:40', badge:'One-Way'},
        {route:'Geneva &#8594; Dubai', cat:'Heavy Jet', price:'~38% below charter rate', window:'Departs tomorrow, AM', badge:'Time-Sensitive'},
        {route:'Miami &#8594; New York', cat:'Light Jet', price:'~50% below charter rate', window:'Departs today, 21:15', badge:'One-Way'},
        {route:'London &#8594; Ibiza', cat:'Midsize Jet', price:'~40% below charter rate', window:'Departs in 3 days', badge:'Flexible Window'},
        {route:'Los Angeles &#8594; Aspen', cat:'Super-Midsize', price:'~35% below charter rate', window:'Departs tomorrow, PM', badge:'Time-Sensitive'},
        {route:'Paris &#8594; Milan', cat:'Light Jet', price:'~48% below charter rate', window:'Departs today, 16:00', badge:'One-Way'},
        {route:'Dubai &#8594; Riyadh', cat:'Midsize Jet', price:'~42% below charter rate', window:'Departs in 2 days', badge:'Flexible Window'},
        {route:'New York &#8594; Palm Beach', cat:'Light Jet', price:'~44% below charter rate', window:'Departs today, 19:30', badge:'One-Way'}
      ];

      function cardHTML(o){
        return '<div class="legs-ticker-card">' +
          '<div class="legs-ticker-route">' + o.route + '</div>' +
          '<div class="legs-ticker-meta"><span>' + o.cat + '</span><span class="legs-ticker-price">' + o.price + '</span></div>' +
          '<div class="legs-ticker-meta"><span>' + o.window + '</span></div>' +
          '<span class="legs-ticker-badge">' + o.badge + '</span>' +
        '</div>';
      }

      // Duplicate the list once so the CSS scroll (translateX -50%) loops seamlessly
      var html = opportunities.map(cardHTML).join('') + opportunities.map(cardHTML).join('');
      track.innerHTML = html;
    })();

    // ===== IN MOTION =====
    (function(){
      var counterEl = document.getElementById('imCounter');
      var eyebrowEl = document.getElementById('imEyebrow');
      var titleEl = document.getElementById('imTitle');
      var textEl = document.getElementById('imText');
      var prevBtn = document.getElementById('imPrev');
      var nextBtn = document.getElementById('imNext');
      var videoEl = document.getElementById('imVideo');
      if(!counterEl) return;

      var moments = [
        {eyebrow:'Airport Arrival', title:'Arrival', text:'The first moments on the ground \u2014 a private terminal, a car already waiting.', video:'videos/im-arrival.mp4', cat:'arrivals'},
        {eyebrow:'Chauffeur Collection', title:'Collection', text:'The handover from air to road, timed so nothing is left waiting.', video:'videos/im-collection.mp4', cat:'ground'},
        {eyebrow:'Aircraft Boarding', title:'Boarding', text:'The walk to the aircraft \u2014 no queue, no gate, no delay.', video:'videos/im-boarding.mp4', cat:'aviation'},
        {eyebrow:'Inflight Experience', title:'Onboard', text:'What the cabin actually looks like once the door is closed.', video:'videos/im-onboard.mp4', cat:'aviation'},
        {eyebrow:'Concierge Arrangement', title:'Arranged In Advance', text:'The details settled before a guest ever has to ask.', video:'videos/im-arranged.mp4', cat:'concierge'},
        {eyebrow:'Destination Arrival', title:'Touchdown', text:'The moment a journey becomes a place.', video:'videos/im-touchdown.mp4', cat:'arrivals'},
        {eyebrow:'Lifestyle Access', title:'Beyond The Flight', text:'What a Jettset journey opens up once the aircraft is behind you.', video:'videos/im-beyond.mp4', cat:'concierge'},
        {eyebrow:'Events & Hospitality', title:'On The Ground', text:'Where a journey leads \u2014 and what is waiting there.', video:'videos/im-ground.mp4', cat:'culture'}
      ];

      var activeCat = 'all';
      var filtered = moments.map(function(m,i){ return i; });
      var index = 0;

      function applyFilter(cat){
        activeCat = cat;
        filtered = moments
          .map(function(m,i){ return {m:m, i:i}; })
          .filter(function(o){ return cat === 'all' || o.m.cat === cat; })
          .map(function(o){ return o.i; });
        if(!filtered.length) filtered = moments.map(function(m,i){ return i; });
        index = 0;
        render();
      }

      function render(){
        var realIndex = filtered[index];
        var m = moments[realIndex];
        counterEl.textContent = String(index + 1).padStart(2, '0') + ' / ' + String(filtered.length).padStart(2, '0');
        eyebrowEl.textContent = m.eyebrow;
        titleEl.textContent = m.title;
        textEl.textContent = m.text;
        if(videoEl){
          videoEl.src = m.video;
          videoEl.load();
          videoEl.play().catch(function(){}); // autoplay can be blocked pre-interaction; harmless if so
        }
      }
      prevBtn.addEventListener('click', function(){
        index = (index - 1 + filtered.length) % filtered.length;
        render();
      });
      nextBtn.addEventListener('click', function(){
        index = (index + 1) % filtered.length;
        render();
      });

      // Wire the filter tag row (All / Aviation / Arrivals / Ground / Concierge / Culture)
      var tagMap = {'All':'all','Aviation':'aviation','Arrivals':'arrivals','Ground':'ground','Concierge':'concierge','Culture':'culture'};
      var tags = document.querySelectorAll('.pillar-tags .pillar-tag');
      tags.forEach(function(tag){
        tag.addEventListener('click', function(){
          tags.forEach(function(t){ t.classList.remove('is-active'); });
          tag.classList.add('is-active');
          var cat = tagMap[tag.textContent.trim()] || 'all';
          applyFilter(cat);
        });
      });

      // Wire Featured Films cards to jump straight to that film
      var filmTitleToMoment = {
        'The Departure':'Collection',
        'The Arrival':'Arrival',
        'The Ground Movement':'Collection',
        'The Destination':'Touchdown',
        'The House':'Beyond The Flight'
      };
      document.querySelectorAll('.pillars .pillar-card').forEach(function(card){
        var h = card.querySelector('.eyebrow');
        if(!h || !filmTitleToMoment[h.textContent.trim()]) return;
        card.style.cursor = 'pointer';
        card.addEventListener('click', function(){
          var wantTitle = filmTitleToMoment[h.textContent.trim()];
          var realIdx = moments.findIndex(function(m){ return m.title === wantTitle; });
          if(realIdx === -1) return;
          // Reset to "All" so the target film is reachable regardless of active filter
          tags.forEach(function(t){ t.classList.remove('is-active'); });
          var allTag = Array.prototype.find ? tags[0] : null;
          if(tags[0]) tags[0].classList.add('is-active');
          applyFilter('all');
          index = filtered.indexOf(realIdx);
          render();
          document.getElementById('imVideo').scrollIntoView({behavior:'smooth', block:'center'});
        });
      });

      render();
    })();

    // ===== CHARTER ROUTE PREVIEW (highlight reel, auto-playing) =====
    (function(){
      var grid = document.getElementById('routePreviewGrid');
      if(!grid) return;

      var svgNS = 'http://www.w3.org/2000/svg';
      var reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

      // Same underlying data as the homepage Signature Route Map, so figures never drift apart
      var previewRoutes = [
        {label:'London \u2013 Nice \u2013 Monaco', theme:'Riviera Escape', from:'London', to:'Monaco', time:'~1h 40m'},
        {label:'London \u2013 Dubai', theme:'Gulf Crossing', from:'London', to:'Dubai', time:'~6h 45m'},
        {label:'New York \u2013 London', theme:'Atlantic Crossing', from:'New York', to:'London', time:'~7h 15m'}
      ];

      function el(tag, attrs){
        var e = document.createElementNS(svgNS, tag);
        for(var k in attrs){ e.setAttribute(k, attrs[k]); }
        return e;
      }

      previewRoutes.forEach(function(route, i){
        var card = document.createElement('div');
        card.className = 'route-preview-card';

        var svg = el('svg', {'class':'rp-svg', 'viewBox':'0 0 400 110', 'xmlns':svgNS});
        var x1 = 40, y1 = 65, x2 = 360, y2 = 65;
        var d = 'M' + x1 + ',' + y1 + ' Q200,20 ' + x2 + ',' + y2;
        var path = el('path', {'class':'rp-line', 'd':d, 'id':'rpPath' + i});
        var dot1 = el('circle', {'class':'rp-dot', 'cx':x1, 'cy':y1, 'r':4});
        var dot2 = el('circle', {'class':'rp-dot', 'cx':x2, 'cy':y2, 'r':4});
        var label1 = el('text', {'class':'rp-city', 'x':x1, 'y':y1 + 22, 'text-anchor':'middle'});
        label1.textContent = route.from;
        var label2 = el('text', {'class':'rp-city', 'x':x2, 'y':y2 + 22, 'text-anchor':'middle'});
        label2.textContent = route.to;
        var plane = el('path', {'class':'rp-plane', 'd':'M 9,0 L -6,-4.5 L -2,0 L -6,4.5 Z'});

        svg.appendChild(path);
        svg.appendChild(dot1);
        svg.appendChild(dot2);
        svg.appendChild(label1);
        svg.appendChild(label2);
        svg.appendChild(plane);

        var meta = document.createElement('div');
        meta.innerHTML = '<div class="rp-theme">' + route.theme + '</div><div class="rp-meta">' + route.label + ' &middot; ' + route.time + '</div>';

        card.appendChild(svg);
        card.appendChild(meta);
        grid.appendChild(card);

        var length = path.getTotalLength();
        if(reduceMotion){
          var mid = path.getPointAtLength(length * 0.5);
          plane.setAttribute('transform', 'translate(' + mid.x + ',' + mid.y + ')');
          return;
        }

        var duration = 5200;
        var offset = i * 900; // stagger so all three don't move in lockstep
        function frame(ts){
          var t = ((ts + offset) % duration) / duration;
          var point = path.getPointAtLength(t * length);
          var lookahead = path.getPointAtLength(Math.min(t * length + 2, length));
          var angle = Math.atan2(lookahead.y - point.y, lookahead.x - point.x) * (180 / Math.PI);
          plane.setAttribute('transform', 'translate(' + point.x + ',' + point.y + ') rotate(' + angle + ')');
          requestAnimationFrame(frame);
        }
        requestAnimationFrame(frame);
      });
    })();

    // ===== SIGNATURE ROUTE MAP + AIRCRAFT ANIMATION =====
    (function(){
      var svg = document.getElementById('rmSvg');
      var chipList = document.getElementById('rmChipList');
      var caption = document.getElementById('rmCaption');
      if(!svg || !chipList || !caption) return;

      var reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      var svgNS = 'http://www.w3.org/2000/svg';

      var cities = {
        la:{label:'Los Angeles', x:110, y:290},
        aspen:{label:'Aspen', x:190, y:230},
        nyc:{label:'New York', x:290, y:310},
        london:{label:'London', x:700, y:200},
        paris:{label:'Paris', x:720, y:215},
        nice:{label:'Nice', x:755, y:265},
        monaco:{label:'Monaco', x:768, y:270},
        ibiza:{label:'Ibiza', x:730, y:320},
        mykonos:{label:'Mykonos', x:920, y:285},
        dubai:{label:'Dubai', x:1130, y:335},
        mumbai:{label:'Mumbai', x:1240, y:390},
        singapore:{label:'Singapore', x:1480, y:445},
        'hong-kong':{label:'Hong Kong', x:1450, y:340},
        lagos:{label:'Lagos', x:700, y:430}
      };

      var routes = [
        {id:'r1', label:'London \u2013 Nice \u2013 Monaco', theme:'Riviera Escape', cityKeys:['london','nice','monaco'], time:'~1h 40m', aircraft:'Light Jet', note:'A classic Riviera routing \u2014 Nice for the coastline, Monaco a short transfer beyond.'},
        {id:'r2', label:'London \u2013 Ibiza', theme:'Balearic Escape', cityKeys:['london','ibiza'], time:'~2h 20m', aircraft:'Midsize Jet', note:'One of our most requested summer routes, sourced fresh for each departure.'},
        {id:'r3', label:'London \u2013 Dubai', theme:'Gulf Crossing', cityKeys:['london','dubai'], time:'~6h 45m', aircraft:'Super-Midsize / Long Range', note:'A single overnight leg, with cabin rest arranged to match your arrival.'},
        {id:'r4', label:'Paris \u2013 Mykonos', theme:'Aegean Escape', cityKeys:['paris','mykonos'], time:'~2h 50m', aircraft:'Midsize Jet', note:'Island-hopping without the layovers \u2014 direct, discreet, on your schedule.'},
        {id:'r5', label:'New York \u2013 London', theme:'Atlantic Crossing', cityKeys:['nyc','london'], time:'~7h 15m', aircraft:'Long Range', note:'The Atlantic crossing, reimagined without a commercial terminal in sight.'},
        {id:'r6', label:'Los Angeles \u2013 Aspen', theme:'Aspen Escape', cityKeys:['la','aspen'], time:'~1h 45m', aircraft:'Light Jet', note:'A ski-season staple \u2014 timed around the mountain, not a commercial schedule.'},
        {id:'r7', label:'Dubai \u2013 Mumbai', theme:'Arabian Sea Crossing', cityKeys:['dubai','mumbai'], time:'~3h 15m', aircraft:'Midsize Jet', note:'A short crossing connecting two of our most active regions, sourced operator-direct.'},
        {id:'r8', label:'Singapore \u2013 Hong Kong', theme:'Asia Express', cityKeys:['singapore','hong-kong'], time:'~3h 45m', aircraft:'Midsize Jet', note:'The classic Asia-Pacific business corridor, without the layover.'},
        {id:'r9', label:'Lagos \u2013 London', theme:'Lagos Crossing', cityKeys:['lagos','london'], time:'~6h 30m', aircraft:'Super-Midsize / Long Range', note:'One of our most active corridors \u2014 sourced with the same discretion and speed, wherever the journey begins.'},
        {id:'r10', label:'Lagos \u2013 New York', theme:'Atlantic Gateway', cityKeys:['lagos','nyc'], time:'~10h 30m', aircraft:'Long Range', note:'A single long-range leg connecting two of our most engaged guest bases, without a stop in between.'}
      ];

      function pathFromKeys(keys){
        var pts = keys.map(function(k){ return [cities[k].x, cities[k].y]; });
        var d = 'M' + pts[0][0] + ',' + pts[0][1];
        for(var i = 0; i < pts.length - 1; i++){
          var x1 = pts[i][0], y1 = pts[i][1], x2 = pts[i+1][0], y2 = pts[i+1][1];
          var mx = (x1 + x2) / 2, my = (y1 + y2) / 2;
          var dx = x2 - x1, dy = y2 - y1;
          var dist = Math.sqrt(dx*dx + dy*dy) || 1;
          var offset = -dist * 0.14;
          var nx = -dy / dist, ny = dx / dist;
          var cx = mx + nx * offset, cy = my + ny * offset;
          d += ' Q' + cx + ',' + cy + ' ' + x2 + ',' + y2;
        }
        return d;
      }

      function el(tag, attrs){
        var e = document.createElementNS(svgNS, tag);
        for(var k in attrs){ e.setAttribute(k, attrs[k]); }
        return e;
      }

      // Draw routes (behind dots)
      routes.forEach(function(route){
        var path = el('path', {
          'class':'rm-route',
          'data-route-id':route.id,
          'd':pathFromKeys(route.cityKeys)
        });
        svg.appendChild(path);
        route.pathEl = path;
      });

      // Draw unique city dots + labels
      var drawnCities = {};
      Object.keys(cities).forEach(function(key){
        var c = cities[key];
        var dot = el('circle', {'class':'rm-city-dot','data-city':key,'cx':c.x,'cy':c.y,'r':5});
        var label = el('text', {'class':'rm-city-label','data-city':key,'x':c.x + 10,'y':c.y - 10});
        label.textContent = c.label;
        svg.appendChild(dot);
        svg.appendChild(label);
        drawnCities[key] = {dot:dot, label:label};
      });

      // Plane marker
      var plane = el('path', {
        'class':'rm-plane',
        'id':'rmPlane',
        'd':'M 14,0 L -9,-7 L -3,0 L -9,7 Z'
      });
      svg.appendChild(plane);

      // Chips
      routes.forEach(function(route){
        var chip = document.createElement('button');
        chip.type = 'button';
        chip.className = 'rm-chip';
        chip.textContent = route.label;
        chip.setAttribute('data-route-id', route.id);
        chip.addEventListener('click', function(){ selectRoute(route.id); });
        chipList.appendChild(chip);
        route.chipEl = chip;
      });
      routes.forEach(function(route){
        route.pathEl.addEventListener('click', function(){ selectRoute(route.id); });
      });

      var activeRouteId = null;
      var rafId = null;

      function selectRoute(routeId){
        if(activeRouteId === routeId) return;
        activeRouteId = routeId;
        var route = routes.filter(function(r){ return r.id === routeId; })[0];
        if(!route) return;

        routes.forEach(function(r){
          r.pathEl.classList.toggle('is-active', r.id === routeId);
          r.chipEl.classList.toggle('is-active', r.id === routeId);
        });
        Object.keys(drawnCities).forEach(function(key){
          var isInRoute = route.cityKeys.indexOf(key) !== -1;
          drawnCities[key].dot.classList.toggle('is-active', isInRoute);
          drawnCities[key].label.classList.toggle('is-active', isInRoute);
        });

        caption.querySelector('.eyebrow').textContent = route.label;
        caption.querySelector('h4').textContent = route.theme || 'A Signature Journey';
        caption.querySelector('p').textContent = route.note;
        var mobileRouteName = document.getElementById('rmMobileRouteName');
        if(mobileRouteName) mobileRouteName.textContent = route.label;
        var existingMeta = caption.querySelector('.rm-meta');
        if(existingMeta) existingMeta.remove();
        var meta = document.createElement('div');
        meta.className = 'rm-meta';
        meta.innerHTML = '<span>Flight Time <strong>' + route.time + '</strong></span><span>Aircraft <strong>' + route.aircraft + '</strong></span>';
        caption.appendChild(meta);

        animatePlane(route.pathEl);
      }

      function animatePlane(pathEl){
        if(rafId) cancelAnimationFrame(rafId);
        var length = pathEl.getTotalLength();
        plane.classList.add('is-active');

        if(reduceMotion){
          var mid = pathEl.getPointAtLength(length * 0.5);
          plane.setAttribute('transform', 'translate(' + mid.x + ',' + mid.y + ')');
          return;
        }

        var duration = 4200;
        var startTime = null;
        function frame(ts){
          if(!startTime) startTime = ts;
          var elapsed = (ts - startTime) % (duration + 900);
          var t = Math.min(elapsed / duration, 1);
          var point = pathEl.getPointAtLength(t * length);
          var lookahead = pathEl.getPointAtLength(Math.min(t * length + 2, length));
          var angle = Math.atan2(lookahead.y - point.y, lookahead.x - point.x) * (180 / Math.PI);
          plane.setAttribute('transform', 'translate(' + point.x + ',' + point.y + ') rotate(' + angle + ')');
          rafId = requestAnimationFrame(frame);
        }
        rafId = requestAnimationFrame(frame);
      }

      // Default selection so the map isn't inert on load
      selectRoute('r3');
    })();

    // ===== POLISH: MAGNETIC BUTTONS =====
    (function(){
      var reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      var isFinePointer = window.matchMedia('(hover: hover) and (pointer: fine)').matches;
      if(reduceMotion || !isFinePointer) return;

      // Magnetic buttons — subtle pull toward cursor on approach
      document.querySelectorAll('.btn-ghost').forEach(function(btn){
        btn.addEventListener('mousemove', function(e){
          var rect = btn.getBoundingClientRect();
          var x = e.clientX - (rect.left + rect.width / 2);
          var y = e.clientY - (rect.top + rect.height / 2);
          btn.style.transform = 'translate(' + (x * 0.18) + 'px,' + (y * 0.35) + 'px)';
        });
        btn.addEventListener('mouseleave', function(){
          btn.style.transform = 'translate(0,0)';
        });
      });
    })();

    // ===== PARALLAX DEPTH (chapter hero "3D touch") =====
    (function(){
      var reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      var layers = document.querySelectorAll('.chapter-hero-parallax');
      if(reduceMotion || !layers.length) return;

      var ticking = false;
      function updateParallax(){
        layers.forEach(function(layer){
          var hero = layer.closest('.chapter-hero');
          if(!hero) return;
          var rect = hero.getBoundingClientRect();
          if(rect.bottom < 0 || rect.top > window.innerHeight) return;
          var offset = rect.top * 0.12;
          layer.style.transform = 'translateY(' + offset + 'px)';
        });
        ticking = false;
      }
      window.addEventListener('scroll', function(){
        if(!ticking){ requestAnimationFrame(updateParallax); ticking = true; }
      }, {passive:true});
      updateParallax();
    })();

    // ===== SITEWIDE SCROLL REVEAL =====
    // Same IntersectionObserver pattern already proven on the Journey
    // Builder timeline above -- generalized so any page can opt an element
    // in with [data-reveal], rather than every page reinventing its own
    // reveal logic. Runs once per element, then unobserves (no repeat cost).
    (function(){
      var revealEls = document.querySelectorAll('[data-reveal]');
      if(!revealEls.length) return;

      if('IntersectionObserver' in window){
        var revealObserver = new IntersectionObserver(function(entries){
          entries.forEach(function(entry){
            if(entry.isIntersecting){
              entry.target.classList.add('is-revealed');
              revealObserver.unobserve(entry.target);
            }
          });
        }, {threshold:0.2, rootMargin:'0px 0px -8% 0px'});
        revealEls.forEach(function(el){ revealObserver.observe(el); });
      } else {
        revealEls.forEach(function(el){ el.classList.add('is-revealed'); });
      }
    })();

    // ===== CINEMATIC HERO: film reveal + ambient cursor glow =====
    (function(){
      var heroSection = document.querySelector('.hero');
      var heroFilm = document.getElementById('heroFilm');
      var heroGlow = document.getElementById('heroGlow');
      if(!heroSection) return;

      var reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

      // Film loops continuously; copy enters early on staggered one-way
      // latches (0.5s / 0.9s / 1.5s / 2.1s after playback starts) and then
      // remains stable — nothing resets when the video loops.
      if(heroFilm){
        var CUES = [
          {cls:'r-kicker', at: 400},
          {cls:'r-title',  at: 700},
          {cls:'r-sub',    at:1600},
          {cls:'r-cta',    at:2000}
        ];
        var started = false;
        function revealAll(){
          CUES.forEach(function(c){ heroSection.classList.add(c.cls); });
        }
        function startCues(){
          if(started) return;
          started = true;
          CUES.forEach(function(c){
            setTimeout(function(){ heroSection.classList.add(c.cls); }, c.at);
          });
        }
        heroFilm.addEventListener('playing', startCues);
        if(reduceMotion){
          heroFilm.removeAttribute('autoplay');
          heroFilm.removeAttribute('loop');
          heroFilm.pause();
          revealAll();
        } else {
          var playAttempt = heroFilm.play();
          if(playAttempt && playAttempt.catch){ playAttempt.catch(revealAll); }
        }
        setTimeout(revealAll, 6000); // failsafe: copy never stays hidden
      }

      if(reduceMotion) return;

      // Ambient gold glow follows cursor — desktop pointer devices only
      var isFinePointer = window.matchMedia('(hover: hover) and (pointer: fine)').matches;
      if(isFinePointer && heroGlow){
        heroSection.addEventListener('mousemove', function(e){
          var rect = heroSection.getBoundingClientRect();
          var x = ((e.clientX - rect.left) / rect.width) * 100;
          var y = ((e.clientY - rect.top) / rect.height) * 100;
          heroGlow.style.setProperty('--gx', x + '%');
          heroGlow.style.setProperty('--gy', y + '%');
          heroGlow.classList.add('is-active');
        });
        heroSection.addEventListener('mouseleave', function(){
          heroGlow.classList.remove('is-active');
        });
      }
    })();
    // ===== BUILD YOUR JOURNEY CONFIGURATOR =====
    (function(){
      var card = document.querySelector('.jb-card');
      if(!card) return;

      var state = { from:'', to:'', depart:'', tripType:'oneway', returnDate:'', adults:1, children:0, occasion:'', prefs:[] };
      var currentStep = 1;
      var totalSteps = 4;

      var fromInput = document.getElementById('jbFrom');
      var toInput = document.getElementById('jbTo');
      var backBtn = document.getElementById('jbBack');
      var nextBtn = document.getElementById('jbNextBtn');
      var progressSteps = document.querySelectorAll('.jb-progress-step');
      var panels = document.querySelectorAll('.jb-panel');
      var summaryCard = document.getElementById('jbSummaryCard');
      var ticketHeader = document.getElementById('jbTicketHeader');
      var ticketTheme = document.getElementById('jbTicketTheme');
      var ticketFrom = document.getElementById('jbTicketFrom');
      var ticketTo = document.getElementById('jbTicketTo');
      var progressFill = document.getElementById('jbProgressFill');

      // Themed titles — only for recognised signature routes; custom entries stay plain
      var routeThemes = {
        'london|monaco':'Riviera Escape', 'monaco|london':'Riviera Escape',
        'london|ibiza':'Balearic Escape', 'ibiza|london':'Balearic Escape',
        'london|dubai':'Gulf Crossing', 'dubai|london':'Gulf Crossing',
        'paris|mykonos':'Aegean Escape', 'mykonos|paris':'Aegean Escape',
        'new york|london':'Atlantic Crossing', 'london|new york':'Atlantic Crossing',
        'los angeles|aspen':'Aspen Escape', 'aspen|los angeles':'Aspen Escape',
        'dubai|mumbai':'Arabian Sea Crossing', 'mumbai|dubai':'Arabian Sea Crossing',
        'singapore|hong kong':'Asia Express', 'hong kong|singapore':'Asia Express',
        'lagos|london':'Lagos Crossing', 'london|lagos':'Lagos Crossing',
        'lagos|new york':'Atlantic Gateway', 'new york|lagos':'Atlantic Gateway'
      };

      function lookupTheme(from, to){
        if(!from || !to) return null;
        var key = from.trim().toLowerCase() + '|' + to.trim().toLowerCase();
        return routeThemes[key] || null;
      }

      function updateTicketHeader(){
        ticketFrom.textContent = state.from || 'Departure';
        ticketFrom.classList.toggle('jb-ticket-empty', !state.from);
        ticketTo.textContent = state.to || 'Destination';
        ticketTo.classList.toggle('jb-ticket-empty', !state.to);
        ticketHeader.classList.toggle('is-visible', !!(state.from || state.to));

        var theme = lookupTheme(state.from, state.to);
        if(theme){
          ticketTheme.textContent = theme;
          ticketTheme.classList.add('is-visible');
          ticketHeader.classList.add('has-theme');
        } else {
          ticketTheme.textContent = '';
          ticketTheme.classList.remove('is-visible');
          ticketHeader.classList.remove('has-theme');
        }
      }

      function showStep(n){
        currentStep = n;
        panels.forEach(function(p){
          p.classList.toggle('is-active', parseInt(p.getAttribute('data-panel'),10) === n);
        });
        progressSteps.forEach(function(s){
          var stepNum = parseInt(s.getAttribute('data-step'),10);
          s.classList.toggle('is-active', stepNum === n);
          s.classList.toggle('is-done', stepNum < n);
        });
        progressFill.style.width = (((n - 1) / (totalSteps - 1)) * 100) + '%';
        backBtn.disabled = (n === 1);
        nextBtn.textContent = (n === totalSteps) ? 'Continue Your Journey' : 'Continue';
        if(n === totalSteps){ renderSummary(); }
      }

      function renderSummary(){
        var prefsText = state.prefs.length ? state.prefs.join(', ') : 'Not yet decided';
        var travellers = state.adults + (state.adults === 1 ? ' Adult' : ' Adults') + (state.children ? ', ' + state.children + (state.children === 1 ? ' Child' : ' Children') : '');
        var dateText = state.depart ? state.depart : 'Flexible';
        if(state.tripType === 'roundtrip' && state.returnDate){ dateText += ' → ' + state.returnDate; }
        else if(state.tripType === 'roundtrip'){ dateText += ' (Return)'; }
        var rows = [
          ['Route', (state.from || 'Open') + ' → ' + (state.to || 'Open')],
          ['Date', dateText],
          ['Travellers', travellers],
          ['Shaping This Journey', state.occasion || 'Not specified'],
          ['Begin Arranging', prefsText]
        ];
        summaryCard.innerHTML = rows.map(function(r){
          return '<div class="jb-summary-row"><span class="jb-summary-label">' + r[0] + '</span><span class="jb-summary-value">' + r[1] + '</span></div>';
        }).join('');
      }

      // Quick route chips
      document.querySelectorAll('.jb-chip').forEach(function(chip){
        chip.addEventListener('click', function(){
          fromInput.value = chip.getAttribute('data-from');
          toInput.value = chip.getAttribute('data-to');
          state.from = fromInput.value;
          state.to = toInput.value;
          updateTicketHeader();
        });
      });
      fromInput.addEventListener('input', function(){ state.from = fromInput.value; updateTicketHeader(); });
      toInput.addEventListener('input', function(){ state.to = toInput.value; updateTicketHeader(); });

      // Departure / return date
      var departInput = document.getElementById('jbDepart');
      var returnDateInput = document.getElementById('jbReturnDate');
      if(departInput) departInput.addEventListener('input', function(){ state.depart = departInput.value; });
      if(returnDateInput) returnDateInput.addEventListener('input', function(){ state.returnDate = returnDateInput.value; });

      // One way / Return toggle
      var tripToggle = document.getElementById('jbTripToggle');
      var returnField = document.getElementById('jbReturnField');
      if(tripToggle){
        tripToggle.querySelectorAll('button').forEach(function(btn){
          btn.addEventListener('click', function(){
            tripToggle.querySelectorAll('button').forEach(function(b){ b.classList.remove('is-active'); });
            btn.classList.add('is-active');
            state.tripType = btn.getAttribute('data-trip');
            if(returnField) returnField.style.display = (state.tripType === 'roundtrip') ? 'flex' : 'none';
          });
        });
      }

      // Passenger steppers (Adults / Children)
      document.querySelectorAll('.jb-passenger-stepper').forEach(function(stepper){
        var key = stepper.getAttribute('data-counter');
        var span = stepper.querySelector('.jb-stepper span');
        stepper.querySelectorAll('button').forEach(function(btn){
          btn.addEventListener('click', function(){
            var action = this.getAttribute('data-action');
            var min = (key === 'adults') ? 1 : 0;
            if(action === 'inc') state[key]++;
            else state[key] = Math.max(min, state[key] - 1);
            span.textContent = state[key];
          });
        });
      });

      // Occasion — single select
      document.querySelectorAll('#jbOccasionGrid .jb-choice').forEach(function(btn){
        btn.addEventListener('click', function(){
          document.querySelectorAll('#jbOccasionGrid .jb-choice').forEach(function(b){ b.classList.remove('is-selected'); });
          btn.classList.add('is-selected');
          state.occasion = btn.getAttribute('data-occasion');
        });
      });

      // Preferences — multi select
      document.querySelectorAll('#jbPrefGrid .jb-choice').forEach(function(btn){
        btn.addEventListener('click', function(){
          var val = btn.getAttribute('data-pref');
          btn.classList.toggle('is-selected');
          if(btn.classList.contains('is-selected')){
            if(state.prefs.indexOf(val) === -1) state.prefs.push(val);
          } else {
            state.prefs = state.prefs.filter(function(p){ return p !== val; });
          }
        });
      });

      backBtn.addEventListener('click', function(){
        if(currentStep > 1) showStep(currentStep - 1);
      });

      nextBtn.addEventListener('click', function(){
        if(currentStep < totalSteps){
          showStep(currentStep + 1);
          return;
        }
        // Final step — hand off to the quote page, pre-filled via sessionStorage.
        // Nothing entered here is lost: every field maps onto its quote.html
        // counterpart, which remains the actual point of enquiry capture.
        if(state.from) sessionStorage.setItem('prefill_qFrom', state.from);
        if(state.to) sessionStorage.setItem('prefill_qTo', state.to);
        if(state.depart) sessionStorage.setItem('prefill_qDepart', state.depart);
        if(state.tripType === 'roundtrip') sessionStorage.setItem('prefill_qTrip', 'roundtrip');
        if(state.returnDate) sessionStorage.setItem('prefill_qReturn', state.returnDate);
        sessionStorage.setItem('prefill_qAdults', String(state.adults));
        if(state.children) sessionStorage.setItem('prefill_qChildren', String(state.children));
        var noteParts = [];
        if(state.occasion) noteParts.push('Shaping this journey: ' + state.occasion);
        if(state.prefs.length) noteParts.push('Please begin arranging: ' + state.prefs.join(', '));
        if(noteParts.length) sessionStorage.setItem('prefill_qNotes', noteParts.join('. '));
        window.jtNavigate('quote.html');
      });

      showStep(1);
    })();

    // ===== MULTI-PAGE BOOTSTRAP (added during page-split) =====
    (function(){
      // Manifest article page: render article from ?id= query param
      if(document.getElementById('page-article')){
        var params = new URLSearchParams(window.location.search);
        var articleId = params.get('id');
        if(articleId && typeof openArticle === 'function'){
          openArticle(articleId);
        }
      }
      // Quote page: apply any pre-fill values stashed by other pages, then clear them.
      // Extended to carry the Journey Builder's date/trip/traveller fields across
      // too, so nothing entered there is lost on handoff. Existing quote-page
      // controls (trip toggle, passenger steppers) are triggered via their own
      // click handlers rather than duplicated, so quote.html's logic stays the
      // single source of truth and is not otherwise modified.
      var qTo = document.getElementById('qTo');
      if(qTo){
        var prefillTo = sessionStorage.getItem('prefill_qTo');
        var prefillFrom = sessionStorage.getItem('prefill_qFrom');
        var prefillNotes = sessionStorage.getItem('prefill_qNotes');
        var prefillDepart = sessionStorage.getItem('prefill_qDepart');
        var prefillTrip = sessionStorage.getItem('prefill_qTrip');
        var prefillReturn = sessionStorage.getItem('prefill_qReturn');
        var prefillAdults = sessionStorage.getItem('prefill_qAdults');
        var prefillChildren = sessionStorage.getItem('prefill_qChildren');

        if(prefillTo) qTo.value = prefillTo;
        var qFrom = document.getElementById('qFrom');
        if(prefillFrom && qFrom) qFrom.value = prefillFrom;
        var qNotes = document.getElementById('qNotes');
        if(prefillNotes && qNotes) qNotes.value = prefillNotes;

        var qDepart = document.getElementById('qDepart');
        if(prefillDepart && qDepart) qDepart.value = prefillDepart;

        if(prefillTrip === 'roundtrip'){
          var roundTripBtn = document.querySelector('#quoteTripToggle button[data-trip="roundtrip"]');
          if(roundTripBtn) roundTripBtn.click(); // reveals the return field via quote.html's own handler
          var qReturn = document.getElementById('qReturn');
          if(prefillReturn && qReturn) qReturn.value = prefillReturn;
        }

        function clickStepper(key, times){
          var stepper = document.querySelector('.stepper[data-counter="' + key + '"] button[data-action="inc"]');
          if(!stepper) return;
          for(var i=0; i<times && i<20; i++){ stepper.click(); }
        }
        if(prefillAdults){
          var adultsWanted = parseInt(prefillAdults, 10) || 1;
          if(adultsWanted > 1) clickStepper('adults', adultsWanted - 1); // default state is already 1
        }
        if(prefillChildren){
          var childrenWanted = parseInt(prefillChildren, 10) || 0;
          if(childrenWanted > 0) clickStepper('children', childrenWanted);
        }

        sessionStorage.removeItem('prefill_qTo');
        sessionStorage.removeItem('prefill_qFrom');
        sessionStorage.removeItem('prefill_qNotes');
        sessionStorage.removeItem('prefill_qDepart');
        sessionStorage.removeItem('prefill_qTrip');
        sessionStorage.removeItem('prefill_qReturn');
        sessionStorage.removeItem('prefill_qAdults');
        sessionStorage.removeItem('prefill_qChildren');
      }
    })();

    // ===== PAGE TRANSITIONS ("chapter turn") =====
    (function(){
      var veil = document.getElementById('jtVeil');
      var pageEl = document.querySelector('.page.active');
      if(!veil) return;

      var reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      var VEIL_OUT_MS = reduceMotion ? 0 : 180; // time veil takes to cover the screen before navigating away
      var isNavigating = false;

      // ---- INCOMING: reveal this page ----
      function revealPage(){
        // Force a reflow so the transition actually runs (rather than the
        // browser coalescing the class changes into a single paint).
        void veil.offsetWidth;
        veil.classList.add('jt-veil-hidden');
        if(pageEl) pageEl.classList.add('jt-revealed');
        isNavigating = false;
      }
      // 'pageshow' fires on fresh loads AND on back/forward restores from
      // bfcache (where 'DOMContentLoaded' does not re-fire), so this is the
      // one event that reliably covers every way a guest can arrive here.
      window.addEventListener('pageshow', revealPage);

      // ---- OUTGOING: veil the screen, then navigate ----
      function jtNavigate(url){
        if(isNavigating) return; // guards against double-click / double-submit
        if(!url) return;
        isNavigating = true;
        if(reduceMotion){
          window.location.href = url;
          return;
        }
        veil.classList.remove('jt-veil-hidden');
        setTimeout(function(){ window.location.href = url; }, VEIL_OUT_MS);
      }
      window.jtNavigate = jtNavigate; // exposed so other handlers below can reuse it

      // ---- Intercept internal link clicks site-wide ----
      document.addEventListener('click', function(e){
        var link = e.target.closest('a');
        if(!link) return;

        // Let the browser handle: modified clicks (new tab/window), non-left
        // clicks, explicit new-tab targets, external/protocol links.
        if(e.defaultPrevented) return;
        if(e.button !== 0 || e.metaKey || e.ctrlKey || e.shiftKey || e.altKey) return;
        if(link.target && link.target !== '_self') return;

        var href = link.getAttribute('href');
        if(!href || href.indexOf('#') === 0) return; // pure in-page anchors: no veil, let native scroll happen
        if(/^(mailto:|tel:|https?:\/\/|\/\/)/i.test(href)) return; // external / contact links untouched

        var dest = new URL(href, window.location.href);
        if(dest.origin !== window.location.origin) return; // safety net for absolute external URLs

        // Same-document hash link (e.g. staying on this page) \u2014 no veil needed.
        if(dest.pathname === window.location.pathname && dest.hash) return;

        e.preventDefault();
        jtNavigate(dest.pathname + dest.search + dest.hash);
      });
    })();
