(function(){'use strict';
var experience=document.querySelector('[data-aircraft-experience]');
if(experience){var tabs=[].slice.call(experience.querySelectorAll('[data-aircraft-tab]'));var panels=[].slice.call(experience.querySelectorAll('[data-aircraft-panel]'));function activate(index,focus){tabs.forEach(function(tab,i){var active=i===index;tab.classList.toggle('is-active',active);tab.setAttribute('aria-selected',String(active));tab.tabIndex=active?0:-1});panels.forEach(function(panel,i){var active=i===index;panel.hidden=!active;panel.classList.toggle('is-active',active)});if(focus)tabs[index].focus()}tabs.forEach(function(tab,index){tab.addEventListener('click',function(){activate(index,false)});tab.addEventListener('keydown',function(event){if(event.key!=='ArrowRight'&&event.key!=='ArrowLeft')return;event.preventDefault();activate((index+(event.key==='ArrowRight'?1:-1)+tabs.length)%tabs.length,true)})});activate(0,false)}
var search=document.getElementById('medicalFaqSearch');if(search){var items=[].slice.call(document.querySelectorAll('[data-faq-item]'));var empty=document.getElementById('medicalFaqEmpty');search.addEventListener('input',function(){var query=search.value.trim().toLowerCase();var visible=0;items.forEach(function(item){var match=!query||item.textContent.toLowerCase().indexOf(query)>-1;item.hidden=!match;if(match)visible+=1});if(empty)empty.hidden=visible!==0})}
}());