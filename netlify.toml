[build]
  command = "npx drizzle-kit migrate --config drizzle.config.ts && npm run build"
  publish = ".next"

[functions]
  node_bundler = "esbuild"

[functions."partner-stale-sweep"]
  schedule = "*/15 * * * *"

[[plugins]]
  package = "@netlify/plugin-nextjs"
